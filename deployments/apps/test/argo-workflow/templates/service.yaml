apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-workflow.fullname" . }}
  labels:
    {{- include "argo-workflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-workflow.selectorLabels" . | nindent 4 }}
